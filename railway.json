{
  "$schema": "https://raw.githubusercontent.com/railwayapp/cli/main/config.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ecobe-engine && npm install --legacy-peer-deps && npm run build"
  },
  "deploy": {
    "startCommand": "cd ecobe-engine && npm run start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
