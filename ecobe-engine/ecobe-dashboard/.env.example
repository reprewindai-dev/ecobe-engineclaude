# ECOBE Engine API URL (used by Next.js route handler proxy)
ECOBE_API_URL=http://localhost:3000

# Optional: if you want the browser to call the engine directly (not recommended)
NEXT_PUBLIC_ECOBE_API_URL=
